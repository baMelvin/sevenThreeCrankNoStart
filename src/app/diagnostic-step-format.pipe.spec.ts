import { DiagnosticStepFormatPipe } from './diagnostic-step-format.pipe';

describe('DiagnosticStepFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DiagnosticStepFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
